#include "../h/syscall_c.h"



void* mem_alloc(size_t size){
    //size_t rest = size % MEM_BLOCK_SIZE;
    //size_t alloc_size = size;
    //if(rest!=0)
        //alloc_size = size + MEM_BLOCK_SIZE - rest;
    //int numBlocks = alloc_size/MEM_BLOCK_SIZE;
    return 0;

}